# Handover — 16 September 2026

Branch `track-rive-source`, nothing committed yet. Rive cloud file:
https://editor.rive.app/file/character/2576025. Deep detail lives in
`_docs/character-swap-handoff.md`; this is the short version.

## State of the work

- `assets/rive/character.riv` is the **v5 export** and holds everything
  below. Awaiting simulator QA — QA is the only proof (project rule).
- The girl's right-leg bug is **fixed**: the wrap had re-written node 0-1905
  from r=0/sx=-100 to r=180/sy=-100 (same picture, wrong for the rotation
  keys). Restored to r=0/sx=-100/sy=100.
- The `inhale`/`exhale` **event keys were lost and are restored** on
  `Breathe`: Event 0-6753 `inhale` frame 0, Event 0-6754 `exhale` frame
  186, property `trigger` (395). This was the dead breath counter.
- The **cat now has tap animations**: SayHi (head tilt, wide eyes, paw
  wave), both ear twitches, Jump (legs tuck, ears flap, arms swing). Values
  are the girl's keys re-based onto cat rest transforms.
- Flutter side: `SkCharacter` takes `skin` (0 girl, 1 cat) and writes it to
  the view model; Home has a `TODO(temp)` swap button
  (`dashboard_view.dart`). `flutter analyze` clean, all 159 tests pass.

## QA still owed (do first)

`flutter run --dart-define-from-file=env.json`, full restart:

1. Girl: breathing to "Breath 2 of 2" (counter must move), then face /
   ear / tummy taps, watch both legs through the Jump.
2. Swap to cat on Home, repeat the taps, and watch its breathing.

## Traps for the next session

- Event keys are INVISIBLE to `queryKeyFrames`/`modifyKeyFrames` echoes.
  The check is `simulateStateMachine` firing `startBreathe` — the trace
  prints the events. Run it after every editor session, before export.
- Mirrored parts rest at r=180/sy=-100 (girl leg was the same trap): cat
  `ear left` 0-8687, cat `leg-left 2` 0-8901. Key them around 180, not 0.
  After any wrap/group work, audit r/sx/sy against rest-frame keys, not
  just x/y (memory: `rive-wrap-rebases-animation-keys`).
- Export: editor cannot write to disk. Use the curl recipe from
  `export_file`'s error, WITH the `notifications/initialized` call, then
  `cp` over `assets/rive/character.riv`.
- Tap trigger names are stale on purpose (face = `tapTorso`, rest =
  `tapEar`). Read a listener's target, not its name.

## Next after QA passes

- Commit (asset + widget + dashboard + docs), per user's say-so.
- Remove the two `TODO(temp)` blocks when the cat passes.
- Cat still missing: body squash, tail sway, `Idle` blinks.
- Decide if the breathing screen should also offer the cat skin.
